<table class="table table-responsive">
  <thead>
    <tr>
      <th></th>
      <th>Name</th>
      <th>Rarity</th>
      <th>Description</th>
      <th>Type Name</th>
    </tr>
  </thead>
  <tbody>
  {{#each items}}
    <tr>
      <th scope="row">
        <img src="{{imgUrl}}" alt="{{dena.name}}" />
      </th>
      <td>{{dena.name}}</td>
      <td>{{dena.rarity}}</td>
      <td>{{dena.description}}</td>
      <td>{{dena.type_name}}</td>
    </tr>
  {{/each}}
  </tbody>
</table>